<%= form_for @company_survey, html: {multipart: true}, url: url do |f| %>
  <%= f.label :team_name, "What is your team name?" %>
  <%= f.text_field :team_name %>

  <p>
    Please let us know of any pre-hiring requirements that Ada should be aware of before matching Ada students for interviews prior to an internship placement.
  </p>
  <p>
    These might include but are not limited to:
  </p>
  <ol>
    <li>Authorization to work in the United States (i.e. Greencard holder or other status requirement).</li>
    <li>Pre-employment drug screening (for legal or illegal substances in Washington state).</li>
    <li>Any other restrictions or requirements that we may need to be aware of to ensure a successful placement.</li>
  </ol>

  <%= f.label :pre_hiring_requirements, "Pre-hiring requirements" %><abbr title="This field is mandatory">*</abbr>
  <%= f.text_area :pre_hiring_requirements, required: true %>

  <%= f.label :preferred_students,  "Which students are you most interested in interviewing?  (Up to 4, please stack rank.)" %>
  <%= f.text_area :preferred_students, rows: 4, cols: 20 %>

  <% questions.each do |question| %>
    <%= f.label question[:name], question[:text].html_safe %><abbr title="This field is mandatory">*</abbr>
    <!-- TODO: Map these to scores internally. -->
    <fieldset>
      <% question[:answers].each_with_index do |answer, index| %>
        <div class="radio">
          <%= f.radio_button "#{question[:name]}", index, required: true %>
          <%= f.label "#{question[:name]}", answer[:text], value: index %>
        </div>
      <% end %>
    </fieldset>
  <% end %>

  <%= f.submit "Submit", class: "button" %>
<% end %>
