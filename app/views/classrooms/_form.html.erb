<section>
  <%= form_for @classroom, html: {multipart: true} do |f| %>
    <%= f.label :name, "Classroom name" %>
    <%= f.text_field :name %>

    <%= label_tag :interviews_csv %>
    <%= file_field_tag :interviews_csv, accept: 'text/csv' %>

    <%= f.submit class: "button" %>
  <% end %>
</section>
